{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Start executor",
  "description": "Starts Supabase for a specific environment",
  "type": "object",
  "properties": {
    "env": {
      "type": "string",
      "description": "Environment to start (e.g., local, staging, production)"
    }
  },
  "required": ["env"]
}
