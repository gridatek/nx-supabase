{
  "$schema": "https://json-schema.org/schema",
  "$id": "Project",
  "title": "Create a Supabase project",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the Supabase project",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of your Supabase project?"
    },
    "directory": {
      "type": "string",
      "description": "Directory where the project will be created",
      "x-prompt": "Where should the project be created?"
    },
    "environments": {
      "type": "string",
      "description": "Comma-separated list of additional environments to create beyond production and local (e.g., staging,dev)"
    }
  },
  "required": ["name"]
}
