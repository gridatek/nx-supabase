# nx-supabase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-supabase` to build the library.

## Running unit tests

Run `nx test nx-supabase` to execute the unit tests via [Vitest](https://vitest.dev/).
